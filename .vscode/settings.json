{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "database": "${workspaceFolder:utilities}/utilities/database.sqlite",
            "name": "vncs"
        }
    ]
}
